name: Build and Push DockerImage

on:
  push:
    branches: [master]

  workflow_dispatch:
      
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup JDK
      uses: actions/setup-java@v4
      with:
        distribution: temurin
        java-version: 17

    - name: Grant permission
      run: chmod +x service-member/gradlew

    - name: Build with Gradle
      run: service-member/gradlew build -x test

    - name: Build Docker Image
      run: docker build -t service-member:latest ./service-member

    - name: Login DockerHub
      run: docker login -u pasvej@gmail.com -p P@ssw0rd1234

    - name: Push Docker Image
      run: docker push service-member:latest
